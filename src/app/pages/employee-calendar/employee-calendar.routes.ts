import { Routes } from '@angular/router';

import { EmployeeCalendarComponent } from './employee-calendar.component';

export const routes: Routes = [{ path: '', component: EmployeeCalendarComponent }];
