import { Routes } from '@angular/router';
import { TestsComponent } from './tests.component';

export const routes: Routes = [
  {
    path: '',
    component: TestsComponent,
    title: 'Pruebas del sitio'
  }
];
